#include "monty.h"
/**
 * start_vars - Funtion to inicializate and make malloc
 * @vars: Structure element
 * Return: ?
 */
int start_vars(vars *vars)
{
	vars->file = NULL;
	vars->buff = NULL;

	return (EXIT_SUCCESS);
}
